---

---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Editor & Uploader</title>
    <style>
        /* Burada CSS kodlarınızı yerleştirin */
    </style>
</head>
<body>
  <div class="container">
    <div id="dropzone" class="dropzone">Dosyaları veya klasörleri buraya sürükleyin</div>
    <button id="downloadButton">Show Dir</button>
    <button id="createFolder">Create Folder</button>
    <ul id="file-list"></ul>
    <div id="preview-grid" class="preview-grid"></div>
</div>

<div class="container">
    <h1>File Editor</h1>
    <button id="open-file">Open File</button>
    <textarea id="file-content" placeholder="File content will appear here..." readonly></textarea>
    <textarea id="new-content" placeholder="Enter new content to append..."></textarea>
    <button id="save-file">Save File</button>
</div>
<div class="container">
  
  <button id="requestAccessButton">Request Directory Access</button>
  <button id="createFolderButton">Create Folder</button>
</div>
      <script type="module" src="src/lib/dropzone.js" defer></script>
</body>
</html>

<style>
:root {
    --gray-10: hsl(258, 7%, 10%);
    --gray-20: hsl(258, 7%, 20%);
    --gray-30: hsl(258, 7%, 30%);
    --gray-40: hsl(258, 7%, 40%);
    --gray-50: hsl(258, 7%, 50%);
    --gray-60: hsl(258, 7%, 60%);
    --gray-70: hsl(258, 7%, 70%);
    --gray-80: hsl(258, 7%, 80%);
    --gray-90: hsl(258, 7%, 90%);
    --black: #13151A;
    --accent-light: #E0CCFA;
}

body {
    font-family: Arial, sans-serif;
    background-color: var(--gray-10);
    color: var(--gray-80);
    margin: 0;
    padding: 20px;
}

#dropzone {
    width: 100%;
    height: 200px;
    border: 2px dashed var(--accent-light);
    border-radius: 8px;
    display: flex;
    justify-content: center;
    align-items: center;
    color: var(--accent-light);
    font-size: 18px;
    cursor: pointer;
    margin-bottom: 20px;
}

.preview-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
    gap: 16px;
    padding: 16px;
}

.preview-item {
    position: relative;
    width: 150px;
    height: 150px;
    border: 2px solid #ddd;
    border-radius: 8px;
    overflow: hidden;
    background-color: #f8f8f8;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 8px;
    box-sizing: border-box;
    text-align: center;
}

.preview-item img {
    max-width: 100%;
    max-height: 100%;
    object-fit: cover;
}

.preview-item .delete-btn {
    position: absolute;
    top: 8px;
    right: 8px;
    background: rgba(255, 0, 0, 0.7);
    color: white;
    border: none;
    border-radius: 50%;
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    font-size: 14px;
    font-weight: bold;
}

.preview-item .delete-btn:hover {
    background: rgba(255, 0, 0, 0.9);
}

body {
    background-color: #282c34;
    color: #61dafb;
    font-family: 'Roboto', sans-serif;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
    margin: 0;
    padding: 0;
}

.container {
    background-color: #1c1f26;
    border-radius: 8px;
    padding: 20px;
    margin: 10px 0;
    width: 90%;
    max-width: 800px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

h1 {
    color: #61dafb;
    font-size: 24px;
    text-align: center;
}

.dropzone.dragover {
    background-color: #333;
}

textarea {
    width: 100%;
    border: 1px solid #61dafb;
    border-radius: 4px;
    padding: 10px;
    margin: 10px 0;
    font-size: 16px;
    background-color: #1c1f26;
    color: #61dafb;
}

button {
    background-color: #61dafb;
    color: #1c1f26;
    border: none;
    border-radius: 4px;
    padding: 10px 20px;
    font-size: 16px;
    cursor: pointer;
    margin: 5px;
    transition: background-color 0.3s;
}

button:hover {
    background-color: #4cafb0;
}

ul {
    list-style-type: none;
    padding: 0;
}

ul li {
    background-color: #444;
    padding: 10px;
    border-radius: 4px;
    margin: 5px 0;
}

</style>